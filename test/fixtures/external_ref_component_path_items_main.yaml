openapi: "3.0.3"
info:
  title: External Ref Component Path Items Main
  version: "1.0.0"
paths: {}
components:
  pathItems:
    WidgetOps:
      post:
        operationId: createWidget
        requestBody:
          required: true
          content:
            application/json:
              schema:
                $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
        responses:
          "201":
            description: Created
