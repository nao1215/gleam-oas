openapi: "3.0.3"
info:
  title: No Description
  version: "1.0.0"
paths:
  /test:
    get:
      responses:
        200:
          content:
            application/json:
              schema:
                type: string
