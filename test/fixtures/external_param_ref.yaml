openapi: "3.0.0"
info:
  title: External Param Ref Test
  version: 1.0.0
paths:
  /test:
    get:
      parameters:
        - $ref: "external.yaml#/components/parameters/Limit"
      responses:
        "200":
          description: OK
