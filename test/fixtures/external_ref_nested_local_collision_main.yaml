openapi: "3.0.3"
info:
  title: External Ref Nested Local Collision Main
  version: "1.0.0"
paths: {}
components:
  schemas:
    Widget:
      type: object
      required:
        - color
      properties:
        color:
          type: string
    Envelope:
      type: object
      properties:
        payload:
          $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
