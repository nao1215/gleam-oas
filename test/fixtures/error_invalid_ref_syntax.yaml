openapi: "3.0.3"
info:
  title: Bad Refs
  version: "1.0.0"
paths:
  /test:
    get:
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: "not-a-valid-ref"
