openapi: "3.0.3"
info:
  title: External Ref Component Path Items Collision Main
  version: "1.0.0"
paths: {}
components:
  schemas:
    Widget:
      type: object
      required:
        - color
      properties:
        color:
          type: string
  pathItems:
    WidgetOps:
      post:
        operationId: createWidget
        responses:
          "200":
            description: OK
            content:
              application/json:
                schema:
                  $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
