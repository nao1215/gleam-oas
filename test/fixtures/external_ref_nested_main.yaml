openapi: "3.0.3"
info:
  title: External Ref Nested Main
  version: "1.0.0"
paths: {}
components:
  schemas:
    Envelope:
      type: object
      required:
        - payload
      properties:
        payload:
          $ref: "./external_ref_nested_shared.yaml#/components/schemas/Widget"
        note:
          type: string
